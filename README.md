# stwb-openmensa
An openmensa compatible webserver for the Studierendenwerk Bonn

Use `--cache-dir /path/to/cache` to retain fetched menus across restarts. The
directory is created automatically; cached entries expire after one year.
